# React

## useCallback
useCallback is a React Hook that lets you cache a function definition between re-renders. By default, every time a component re-renders, all functions declared inside it are recreated, which can lead to performance issues if those functions are passed to optimized child components.

